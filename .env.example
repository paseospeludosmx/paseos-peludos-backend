# ===== Paseos Peludos - Backend (.env.example) =====
# Copia este archivo a ".env" y rellena los valores reales.

# Servidor
PORT=5000
HOST=0.0.0.0

# Base de datos
MONGO_URI=mongodb+srv://user:pass@cluster.mongodb.net
MONGO_DB=paseos

# Auth
JWT_SECRET=change-me

# CORS (origins permitidos, separados por coma)
# Expo: 19006 (web), 8081 (metro), 5173 (vite/web si lo usas)
FRONT_ORIGIN=http://localhost:19006,http://localhost:8081,http://localhost:5173

# Zona horaria
TZ=America/Mexico_City

# Meta (opcional)
GIT_COMMIT=
NODE_ENV=development


